<!DOCTYPE html>
<html>


<head>

    <link href=".\MoviePages\MovieView.css" rel="stylesheet" type="text/css">

</head>

<body id = "backForPage">

    <header>
    </header>

    <main>
        <div  class="PositionColor">
            <h1>Films</h1>
            
            <% table.forEach(columns => { %>
            
                <table >
                    <thead>
                        <tr>
                            <% Object.getOwnPropertyNames(columns[0]).forEach ( (headName) => { %>
                                <th> <%= headName %>  </th>
                                
                             <% }) %>
                            
                        </tr>
                       
                    </thead>
                    <tbody>
                        <% columns.forEach(function(column, index){ %>
                            <% column = Object.values(column) %>
                            <tr>
                                <% column.forEach((element, index) => { %>
                                    <td> <%= element %> </td>
                               <% }); %>
                              
                            </tr> 
                        <% }); %>
                        

                      

                    </tbody>
                </table>    

           <% }); %>
                        <button>  <a href="/MovieAdd">
                            Добавить фильм
                        </a> </button>
                        

                    </tbody>
                </table>    
                
            
        </div>
    </main>

    <footer>

</body>
</html>